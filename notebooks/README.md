# notebooks

Exploratory analysis lives here. `01_eda_olist.ipynb` walks the dataset:
row counts, order-status funnel, revenue seasonality, delivery-time
distribution and review-score relationships — using the same processed
Parquet the pipeline produces (run `make ingest` first).
