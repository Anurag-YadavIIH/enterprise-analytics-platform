# excel

Analyst workbooks. Build them by following `docs/excel.md` (Power Query over
`data/processed/*.parquet`, data-model pivots, formula starter pack).
Keep logic thin — anything reusable belongs in dbt marts or `sql/queries/`.
