# dashboards

BI assets and exports.

- Power BI: follow `docs/powerbi.md` to build the .pbix (connection,
  relationships, DAX measure pack). Commit exported .pbix/.pbit files here.
- Reference extracts land in `data/warehouse/` via the reporting DAG.
